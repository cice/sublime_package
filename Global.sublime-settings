// Place user-specific overrides in this file, to ensure they're preserved
// when upgrading
{
	"file_exclude_patterns": [".gitignore", ".rspec", ".DS_store"],
	"folder_exclude_patterns": [".idea", ".bundle", ".git"]
}
